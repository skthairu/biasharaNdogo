kind = "web"
previewPath = "/"
title = "Kenya SMEs Organization"
version = "1.0.0"
id = "artifacts/kenya-smes"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25636

[services.development]
run = "pnpm --filter @workspace/kenya-smes run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/kenya-smes", "run", "build" ]
publicDir = "artifacts/kenya-smes/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25636"
BASE_PATH = "/"
